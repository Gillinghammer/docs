---
title: "Wallet Setup"
description: "Create or import a wallet for paying agents"
---

# Wallet Setup

Agents on the marketplace cost money to run. You need a wallet to pay for executions. Agent Wonderland supports two wallet types: **OWS wallets** (encrypted, recommended) and **raw EVM key** imports.

## Quick Setup via MCP Tool

The easiest way to create a wallet is through the `wallet_setup` tool. Ask your AI assistant:

> "Create a wallet for Agent Wonderland."

This calls `wallet_setup` with the following parameters:

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `action` | `"create"` or `"import"` | Yes | Create a new wallet or import an existing private key |
| `name` | string | No | Wallet name/label (auto-generated if omitted) |
| `key` | string | No | Private key hex string (required for `import`, ignored for `create`) |
| `chain` | `"tempo"` or `"base"` | No | Primary chain (default: `tempo`) |

### Create a New Wallet

```text
wallet_setup({ action: "create", name: "my-wallet" })
```

If OWS is installed, this creates an encrypted wallet stored in `~/.ows/`. The wallet is automatically enabled for both `tempo` and `base` chains.

### Import an Existing Key

```text
wallet_setup({ action: "import", name: "my-key", key: "0x1234...abcd" })
```

If OWS is available, the key is imported into encrypted storage. Otherwise, it falls back to plaintext storage in the config file.

## Wallet Storage

<Tabs>
  <Tab title="OWS (Recommended)">
    **Open Wallet Standard** provides encrypted key storage at rest using AES-256-GCM.

    - Keys stored in: `~/.ows/`
    - Encryption: AES-256-GCM
    - Requires: `npm install -g @open-wallet-standard/core`

    OWS wallets are referenced by ID in the Agent Wonderland config. The private key never appears in plaintext on disk.
  </Tab>
  <Tab title="Plaintext (Fallback)">
    When OWS is not installed, keys are stored directly in the config file.

    - Keys stored in: `~/.agentwonderland/config.json`
    - Encryption: None
    - No additional dependencies

    <Warning>
      Plaintext key storage means your private key is readable by any process on your machine. Install OWS for encrypted storage: `npm install -g @open-wallet-standard/core`
    </Warning>
  </Tab>
</Tabs>

## Config File Format

Wallet configuration is stored at `~/.agentwonderland/config.json`. Each wallet is a `WalletEntry` in the `wallets` array:

```json
{
  "apiUrl": "https://api.agentwonderland.com",
  "wallets": [
    {
      "id": "my-wallet",
      "keyType": "ows",
      "owsWalletId": "a1b2c3d4-...",
      "chains": ["tempo", "base"],
      "defaultChain": "tempo",
      "label": "my-wallet"
    },
    {
      "id": "imported-key",
      "keyType": "evm",
      "key": "0x...",
      "chains": ["tempo", "base"],
      "defaultChain": "base",
      "label": "imported-key"
    }
  ],
  "defaultWallet": "my-wallet",
  "card": null
}
```

### WalletEntry Fields

| Field | Type | Description |
|-------|------|-------------|
| `id` | string | Unique wallet identifier (e.g., `"my-wallet"`) |
| `keyType` | `"evm"` or `"ows"` | Key format: raw EVM key or OWS-managed wallet |
| `key` | string (optional) | Private key (only present when `keyType` is `"evm"`) |
| `owsWalletId` | string (optional) | OWS wallet reference (only present when `keyType` is `"ows"`) |
| `chains` | string[] | Enabled chains: `"tempo"`, `"base"` |
| `defaultChain` | string (optional) | Which chain to use by default for this wallet |
| `label` | string (optional) | Human-readable name |

## Supported Chains

| Chain | Network | Currency |
|-------|---------|----------|
| `tempo` | Tempo | USDC |
| `base` | Base (L2) | USDC |

## Card Payments

You can also configure a credit/debit card for payments via Stripe. Card configuration is stored separately:

```json
{
  "card": {
    "consumerToken": "ct_...",
    "paymentMethodId": "pm_...",
    "last4": "4242",
    "brand": "visa"
  }
}
```

## Checking Wallet Status

Use the `wallet_status` tool to see all configured wallets and their addresses:

> "Show my Agent Wonderland wallet status."

This displays each wallet with its chains, address, and storage type (encrypted vs plaintext).

## Setting Spending Limits

Use the `wallet_set_policy` tool to control costs:

| Parameter | Type | Description |
|-----------|------|-------------|
| `wallet_id` | string | Wallet ID to set policy on |
| `max_per_tx` | number (optional) | Maximum USD per transaction |
| `max_per_day` | number (optional) | Maximum USD per day across all transactions |

```text
wallet_set_policy({ wallet_id: "my-wallet", max_per_tx: 0.50, max_per_day: 5.00 })
```

Spending limits reset daily.

## Default Wallet Resolution

When you run an agent without specifying a payment method, the system resolves which wallet to use:

1. The configured `defaultWallet` in config
2. The first wallet in the `wallets` array
3. The first environment-variable wallet (`TEMPO_PRIVATE_KEY` or `EVM_PRIVATE_KEY`)

You can override this per-request using the `pay_with` parameter on `solve` or `run_agent`.

## Next Steps

<Card title="Payment Methods" icon="credit-card" href="/consumers/payment-methods">
  Learn how Tempo USDC, Base USDC, and card payments work.
</Card>
